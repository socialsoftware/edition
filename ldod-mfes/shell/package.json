{
    "name": "shell",
    "version": "1.0.0",
    "main": "server/src/server.js",
    "license": "MIT",
    "type": "module",
    "scripts": {
        "prebuild": "yarn --cwd client install && yarn --cwd shared install",
        "build": "yarn --cwd client build && yarn --cwd shared build",
        "dev": "yarn build && nodemon -r dotenv/config server/src/server.js dotenv_config_path=.env.dev",
        "docker": "yarn build && node -r dotenv/config server/src/server.js dotenv_config_path=.env"
    },
    "dependencies": {
        "async-express-decorator": "^1.1.0",
        "clear-module": "^4.1.2",
        "cors": "^2.8.5",
        "dotenv": "^16.0.1",
        "express": "^4.18.1",
        "multer": "^1.4.5-lts.1",
        "node-html-parser": "^5.3.3",
        "nodemon": "^2.0.18",
        "tar": "^6.1.11"
    }
}
