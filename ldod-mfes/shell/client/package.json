{
  "name": "shell-client",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "if [ -d dist ]; then rm -r dist;fi && rollup -c && cp -r style dist && cp index.html fonts.js dist",
    "dev": "serve -C -l 3000"
  },
  "license": "MIT",
  "devDependencies": {
    "@rollup/plugin-terser": "^0.2.0",
    "acorn": "^8.8.1",
    "acorn-import-assertions": "^1.8.0",
    "rollup": "^2.79.1",
    "rollup-plugin-import-assert": "^2.1.3",
    "rollup-plugin-import-css": "^3.1.0",
    "rollup-plugin-sourcemaps": "^0.6.3",
    "rollup-plugin-terser": "^7.0.2",
    "serve": "^14.1.2"
  }
}
