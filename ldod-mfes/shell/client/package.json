{
	"name": "shell-client",
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"build": "if [ -d dist ]; then rm -r dist;fi && rollup -c && cp -r style dist && cp index.html dist",
		"dev": "serve -C -l 3000"
	},
	"license": "MIT",
	"devDependencies": {
		"rollup": "^2.79.1",
		"rollup-plugin-import-css": "^3.1.0",
		"rollup-plugin-terser": "^7.0.2",
		"serve": "^14.1.2"
	}
}
