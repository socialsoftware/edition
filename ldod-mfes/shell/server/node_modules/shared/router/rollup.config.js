// rollup.config.js
import { terser } from 'rollup-plugin-terser';

export default {
  input: './src/router.js',
  output: [
    {
      dir: '../public',
      format: 'es',
      plugins: [terser()],
    },
  ],
};
