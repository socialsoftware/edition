{
    "name": "user",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "type": "module",
    "scripts": {
        "test": "yarn transpile && jest",
        "start": "trap 'kill 0' SIGNINT; babel src -w -d build & serve -C -l 3002",
        "transpile": "babel src -d transpiled-src",
        "build": "if [ -d build ]; then rm -r build;fi && yarn transpile && rollup -c",
        "pack": "yarn build && ./scripts/pack.sh user",
        "publish-dev": "yarn run pack && ./scripts/publish.sh user user.js  @./dist/user.tgz http://localhost:9000",
        "unpublish-dev": "./scripts/unpublish.sh user http://localhost:9000",
        "publish": "yarn run pack && ./scripts/publish.sh user user.js  @./dist/user.tgz http://localhost",
        "unpublish": "./scripts/unpublish.sh user http://localhost"
    },
    "jest": {
        "moduleNameMapper": {
            "^shared/(.*)$": "<rootDir>/node_modules/shared/dist/$1"
        }
    },
    "devDependencies": {
        "@babel/cli": "^7.18.6",
        "@babel/core": "^7.18.6",
        "@babel/preset-env": "^7.18.6",
        "@babel/preset-react": "^7.18.6",
        "@rollup/plugin-dynamic-import-vars": "^1.4.3",
        "@types/jest": "^28.1.4",
        "acorn-import-assertions": "^1.8.0",
        "babel-jest": "^28.1.2",
        "jest": "^28.1.2",
        "jest-environment-jsdom": "^28.1.2",
        "rollup": "^2.75.7",
        "rollup-plugin-import-assert": "^2.1.0",
        "rollup-plugin-terser": "^7.0.2",
        "serve": "^13.0.2"
    }
}